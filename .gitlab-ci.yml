# This file was automatically generated by GitLab CI Kit
# Generated: 2025-11-20T06:40:50.257Z
# DO NOT EDIT MANUALLY - Changes will be overwritten

variables:
  AWS_REGION: ap-southeast-2
  IMAGE_VERSION: 2025-11-R1
  AWS_ACCOUNT_ID_DEV: 193963110876
  AWS_ACCOUNT_ID_TEST: 600110492501
  AWS_ACCOUNT_ID_SANDBOX: 716909776967
  AWS_ACCOUNT_ID_PROD: 231775585439
  AWS_ACCOUNT_ID_SHARED_SERVICES: 606789157019
  FF_USE_FASTZIP: "true"
  ARTIFACT_COMPRESSION_LEVEL: fast
  CACHE_COMPRESSION_LEVEL: fast
  IMAGE_NAME: base-image-devbox
  DOCKER_REGISTRY: ${AWS_ACCOUNT_ID_SHARED_SERVICES}.dkr.ecr.${AWS_REGION}.amazonaws.com
stages:
  - test
  - deploy
  - e2e
  - external
default:
  id_tokens:
    GITLAB_OIDC_TOKEN:
      aud: https://gitlab.com
  tags:
    - ${RUNNER_TAG}
  image:
    name: ${DOCKER_REGISTRY}/${IMAGE_NAME}:${IMAGE_VERSION}
validate:
  stage: test
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_PIPELINE_SOURCE == "merge_request_event" && $CI_COMMIT_REF_NAME =~ /^(feat|chore|fix|hotfix)\/.*$/
  before_script:
    - source /opt/toolkit/utils/assume-role.sh
  script:
    - devbox install -q
    - devbox run test
  variables:
    AWS_ACCOUNT_ID: ${AWS_ACCOUNT_ID_DEV}
    SECRET_NAME: auth-tooling-dev
deployDev:
  stage: deploy
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
      when: manual
    - if: $CI_PIPELINE_SOURCE == "merge_request_event" && $CI_COMMIT_REF_NAME =~ /^(feat|chore|fix|hotfix)\/.*$/
      when: manual
    - when: never
  before_script:
    - source /opt/toolkit/utils/assume-role.sh
  script:
    - time devbox install -q
    - devbox run deploy_cdk
  variables:
    AWS_ACCOUNT_ID: ${AWS_ACCOUNT_ID_DEV}
    ENVIRONMENT: dev
deploy_stage:
  stage: deploy
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
      when: manual
    - if: $CI_PIPELINE_SOURCE == "merge_request_event" && $CI_COMMIT_REF_NAME =~ /^(feat|chore|fix|hotfix)\/.*$/
      when: manual
    - when: never
  before_script:
    - source /opt/toolkit/utils/assume-role.sh
  script:
    - time devbox install -q
    - devbox run deploy_cdk
  variables:
    AWS_ACCOUNT_ID: ${AWS_ACCOUNT_ID_TEST}
    ENVIRONMENT: stage
deploy_release:
  stage: deploy
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
      when: manual
    - if: $CI_PIPELINE_SOURCE == "merge_request_event" && $CI_COMMIT_REF_NAME =~ /^(feat|chore|fix|hotfix)\/.*$/
      when: manual
    - when: never
  before_script:
    - source /opt/toolkit/utils/assume-role.sh
  script:
    - time devbox install -q
    - devbox run deploy_cdk
  variables:
    AWS_ACCOUNT_ID: ${AWS_ACCOUNT_ID_TEST}
    ENVIRONMENT: release
deploy_perf:
  stage: deploy
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
      when: manual
    - if: $CI_PIPELINE_SOURCE == "merge_request_event" && $CI_COMMIT_REF_NAME =~ /^(feat|chore|fix|hotfix)\/.*$/
      when: manual
    - when: never
  before_script:
    - source /opt/toolkit/utils/assume-role.sh
  script:
    - time devbox install -q
    - devbox run deploy_cdk
  variables:
    AWS_ACCOUNT_ID: ${AWS_ACCOUNT_ID_TEST}
    ENVIRONMENT: perf
deploy_sandbox:
  stage: deploy
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
      when: manual
    - if: $CI_PIPELINE_SOURCE == "merge_request_event" && $CI_COMMIT_REF_NAME =~ /^(feat|chore|fix|hotfix)\/.*$/
      when: manual
    - when: never
  before_script:
    - source /opt/toolkit/utils/assume-role.sh
  script:
    - time devbox install -q
    - devbox run deploy_cdk
  variables:
    AWS_ACCOUNT_ID: ${AWS_ACCOUNT_ID_SANDBOX}
    ENVIRONMENT: sandbox
deploy_prod:
  stage: deploy
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
      when: manual
    - if: $CI_PIPELINE_SOURCE == "merge_request_event" && $CI_COMMIT_REF_NAME =~ /^(feat|chore|fix|hotfix)\/.*$/
      when: manual
    - when: never
  before_script:
    - source /opt/toolkit/utils/assume-role.sh
  script:
    - time devbox install -q
    - devbox run deploy_cdk
  variables:
    AWS_ACCOUNT_ID: ${AWS_ACCOUNT_ID_PROD}
    ENVIRONMENT: prod
e2e:
  stage: e2e
  needs: []
  cache:
    key: playwright-browsers-v1
    paths:
      - ~/.cache/ms-playwright
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
      when: manual
    - if: $CI_PIPELINE_SOURCE == "merge_request_event" && $CI_COMMIT_REF_NAME =~ /^(feat|chore|fix|hotfix)\/.*$/
      when: manual
    - when: never
  script:
    - npm ci
    - npx playwright install --with-deps chromium
    - npx nx run e2e-tests:test:e2e --skip-nx-cache
  allowFailure: true
  variables:
    AWS_ACCOUNT_ID: ${AWS_ACCOUNT_ID_DEV}
    SECRET_NAME: auth-tooling-dev
    IMAGE_NAME: base-image-debian-node-24
snyk:
  stage: external
  needs: []
  beforeScript: []
  artifacts:
    paths:
      - snyk-**.json
      - snyk-**.html
      - snyk-**.txt
    when: always
    expire_in: 30 day
  variables:
    snyk_test_failed: 0
    snyk_code_test_failed: 0
    IMAGE_NAME: base-image-alpine-node-24
  script:
    - ./build/bin/snyk.sh
  rules:
    - if: $CI_COMMIT_REF_NAME =~ /^(feat|chore|fix|feature|test|infra|refactor)\/.*$/
  allowFailure: false
