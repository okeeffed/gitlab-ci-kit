# This file was automatically generated by GitLab CI Kit
# Generated: 2025-11-20T05:41:59.750Z
# DO NOT EDIT MANUALLY - Changes will be overwritten

variables:
    node_version: "20"
stages:
    - build
    - test
    - deploy
default:
    id_tokens:
        gitlab_oidc_token:
            aud: https://gitlab.com
    tags:
        - docker
build:
    stage: build
    image: node:20
    script:
        - npm ci
        - npm run build
    cache:
        key: ${CI_COMMIT_REF_SLUG}
        paths:
            - node_modules/
    artifacts:
        paths:
            - dist/
        expire_in: 1 week
test:
    stage: test
    image: node:20
    script:
        - npm test
    before_script:
        - npm ci
    allow_failure: true
deploy:
    stage: deploy
    image: node:20
    script:
        - npm run deploy
    rules:
        - if: $CI_COMMIT_BRANCH == "main"
        - if: $CI_PIPELINE_SOURCE == "merge_request_event"
          when: manual
    environment:
        name: production
        url: https://example.com
